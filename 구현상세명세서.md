# 게시판 구현 상세 명세서

## 1. 개요
본 문서는 `cloud2` 게시판 애플리케이션의 백엔드 구현 상세 내용을 기록합니다. 표준 Spring MVC 아키텍처를 준수하여 설계되었습니다.

## 2. 시스템 아키텍처
애플리케이션은 **Layered Architecture**를 따르며, 각 계층은 다음과 같은 역할을 수행합니다.

| 계층 (Layer) | 패키지 | 역할 |
| :--- | :--- | :--- |
| **Controller** | `controller` | 클라이언트의 REST 요청 수신, DTO 처리 및 응답 반환 |
| **Service** | `service` | 비즈니스 로직 수행, Domain-DTO 변환, 트랜잭션 관리(개념적) |
| **Repository** | `repository` | 데이터 저장소(ArrayList) 접근 및 관리 (DAO 계층) |
| **Domain** | `domain` | 핵심 데이터 모델 (Entity) 정의 |
| **DTO** | `dto` | 계층 간 데이터 전송을 위한 객체 (보안 및 API 최적화) |
## 3. 주요 구현 내용

### 3.1. 기술 스택
- **Backend**: Java 21, Spring Boot 4.0.6, Gradle, Lombok
- **Frontend**: React 19, Vite 8, Axios, React Router DOM
- **Environment Management**: `.env` (Frontend), `VITE_API_URL` 활용

### 3.2. 프론트엔드 아키텍처 및 주요 기능
- **Routing**: `react-router-dom`을 사용하여 SPA(Single Page Application) 라우팅 구현
    - `/`: 게시글 목록
    - `/posts/:id`: 게시글 상세
    - `/write`: 게시글 작성
    - `/edit/:id`: 게시글 수정
- **API 통신 (Axios)**:
    - `src/api.js`에서 공통 인스턴스 생성 및 인터셉터 적용
    - **Logging Interceptor**: 모든 요청/응답의 URL과 상태 코드를 브라우저 콘솔에 실시간 기록
- **UI/UX**: `Board.css`를 통한 독립적인 게시판 스타일링 및 현대적인 디자인 적용

### 3.3. 데이터 모델 (Domain)
... (생략) ...

- `Post`: 게시글 식별자, 제목, 내용, 작성자, 비밀번호, 생성일, 수정일 포함
- `Todo`: 기존 할 일 모델 (Lombok 적용 완료)

### 3.3. 데이터 접근 계층 (Repository)
- `PostRepository` 인터페이스를 통해 표준 기능 정의
- `MemoryPostRepository`에서 `ArrayList`와 `AtomicLong`을 사용하여 인메모리 저장소 구현

### 3.4. 데이터 전송 객체 (DTO)
- `PostCreateRequest`: 작성 시 필요한 필드 (비밀번호 포함)
- `PostUpdateRequest`: 수정 시 필요한 필드 (비밀번호 포함)
- `PostResponse`: 조회 시 반환할 필드 (**비밀번호 제외**하여 보안 강화)

### 3.4. 데이터 유효성 검사 (Validation)
- **Backend**: `spring-boot-starter-validation`을 사용하여 DTO 레벨에서 유효성 검사 수행
    - 제목(필수, 100자 이내), 내용(필수), 작성자(필수), 비밀번호(필수, 4자 이상)
- **Frontend**: 폼 제출 전 클라이언트 사이드 검증 및 서버 에러(400 Bad Request) 발생 시 상세 메시지 출력

## 4. 현재 진행 상태
- [x] 1단계: 환경 설정 및 연동 확인 완료
- [x] 2단계: 백엔드 아키텍처 고도화 및 API 구현 완료
- [x] 3단계: 프론트엔드 UI 및 라우팅 설정 완료
- [x] 4단계: 프론트엔드-백엔드 기능 연동 완료
- [x] 5단계: 유효성 검사 추가 및 최종 검증 완료
